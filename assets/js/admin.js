import{requireAuth}from'./guard.js';import{renderShell}from'./shell.js';import{supabase,escapeHtml,fmtDate,money,toast}from'./core.js';
const c=await requireAuth({superAdmin:true});if(!c)throw 0;renderShell(c.profile,'dashboard');let orgs=[],positions=[],districts=[];
async function refresh(){const[{data:o},{data:u},{data:p},{data:d},{data:k},{data:s},{data:a}] = await Promise.all([
 supabase.from('organizations').select('*,districts(name)').order('created_at'),supabase.from('profiles').select('*,organizations(short_name),positions(name)').order('created_at',{ascending:false}),supabase.from('positions').select('*').order('name'),supabase.from('districts').select('*,villages(*)').order('name'),supabase.from('keywords').select('*,organizations(short_name)').order('created_at',{ascending:false}).limit(60),supabase.from('sources').select('*,organizations(short_name)').order('created_at',{ascending:false}).limit(60),supabase.from('audit_logs').select('*').order('created_at',{ascending:false}).limit(50)
 ]);orgs=o||[];positions=p||[];districts=d||[];renderOrgs();renderUsers(u||[]);renderCatalogs();renderKeywords(k||[]);renderSources(s||[]);renderAudit(a||[]);renderMetrics(u||[]);fillSelects();}
function renderMetrics(users){const active=orgs.filter(x=>x.service_status==='active').length,suspended=orgs.filter(x=>x.service_status==='suspended').length;document.querySelector('#metrics').innerHTML=[['Aktiv təşkilat',active],['Dayandırılıb',suspended],['İstifadəçilər',users.length],['Aylıq portfel',money(orgs.reduce((a,x)=>a+Number(x.monthly_price||0),0))]].map(([l,n])=>`<div class="card metric"><div class="label">${l}</div><div class="num">${n}</div></div>`).join('')}
function renderOrgs(){document.querySelector('#org-body').innerHTML=orgs.map(o=>`<tr><td><strong>${escapeHtml(o.short_name)}</strong><br><span class="muted">${escapeHtml(o.name)}</span></td><td>${escapeHtml(o.districts?.name||'—')}</td><td><span class="badge ${o.service_status==='active'?'ok':o.service_status==='grace'?'warn':'danger'}">${o.service_status}</span></td><td>${money(o.monthly_price)}</td><td>${o.next_payment_at?fmtDate(o.next_payment_at):'—'}</td><td><div class="toolbar"><button class="btn secondary" data-org-toggle="${o.id}">${o.service_status==='suspended'?'Aktivləşdir':'Dayandır'}</button></div></td></tr>`).join('');document.querySelectorAll('[data-org-toggle]').forEach(b=>b.onclick=()=>toggleOrg(b.dataset.orgToggle));}
function renderUsers(users){document.querySelector('#user-body').innerHTML=users.map(u=>`<tr><td><strong>${escapeHtml((u.first_name||'')+' '+(u.last_name||''))}</strong><br><span class="muted">${escapeHtml(u.email||'')}</span></td><td>${escapeHtml(u.organizations?.short_name||'Sistem')}</td><td>${escapeHtml(u.positions?.name||'—')}</td><td>${escapeHtml(u.system_role)}</td><td><span class="badge ${u.is_active?'ok':'danger'}">${u.is_active?'Aktiv':'Deaktiv'}</span></td><td><div class="toolbar">${u.system_role!=='super_admin'?`<button class="btn secondary" data-user-toggle="${u.id}" data-active="${u.is_active}">${u.is_active?'Blokla':'Aktiv et'}</button><button class="btn ghost" data-reset="${u.auth_user_id}">Şifrə</button>`:''}</div></td></tr>`).join('');document.querySelectorAll('[data-user-toggle]').forEach(b=>b.onclick=()=>toggleUser(b.dataset.userToggle,b.dataset.active==='true'));document.querySelectorAll('[data-reset]').forEach(b=>b.onclick=()=>resetPassword(b.dataset.reset));}
function fillSelects(){const opts='<option value="">Seçin</option>'+orgs.map(o=>`<option value="${o.id}">${escapeHtml(o.short_name)}</option>`).join('');['#keyword-org','#source-org'].forEach(s=>document.querySelector(s).innerHTML=opts);document.querySelector('#position-org').innerHTML='<option value="">Qlobal</option>'+orgs.map(o=>`<option value="${o.id}">${escapeHtml(o.short_name)}</option>`).join('');document.querySelector('#village-district').innerHTML='<option value="">Rayon seçin</option>'+districts.map(d=>`<option value="${d.id}">${escapeHtml(d.name)}</option>`).join('')}
function renderCatalogs(){document.querySelector('#position-list').innerHTML=positions.map(x=>`<span class="badge info" style="margin:4px">${escapeHtml(x.name)}</span>`).join('');document.querySelector('#location-list').innerHTML=districts.map(d=>`<div style="padding:10px 0;border-bottom:1px solid var(--line)"><strong>${escapeHtml(d.name)}</strong><div class="muted" style="margin-top:4px">${(d.villages||[]).map(v=>escapeHtml(v.name)).join(', ')||'Kənd əlavə edilməyib'}</div></div>`).join('')}
function renderKeywords(rows){document.querySelector('#keyword-list').innerHTML=rows.map(x=>`<div style="display:flex;justify-content:space-between;padding:9px 0;border-bottom:1px solid var(--line)"><span>${escapeHtml(x.value)}</span><span class="muted">${escapeHtml(x.organizations?.short_name||'')}</span></div>`).join('')||'<div class="empty">Açar söz yoxdur</div>'}
function renderSources(rows){document.querySelector('#source-list').innerHTML=rows.map(x=>`<div style="padding:9px 0;border-bottom:1px solid var(--line)"><strong>${escapeHtml(x.platform)}</strong> <span class="muted">${escapeHtml(x.organizations?.short_name||'')}</span><br><a class="muted" target="_blank" href="${x.url}">${escapeHtml(x.url)}</a></div>`).join('')||'<div class="empty">Mənbə yoxdur</div>'}
function renderAudit(rows){document.querySelector('#audit-list').innerHTML=rows.map(x=>`<div style="padding:10px 0;border-bottom:1px solid var(--line)"><strong>${escapeHtml(x.action)}</strong><br><span class="muted">${fmtDate(x.created_at)} • ${escapeHtml(x.actor_email||'sistem')}</span></div>`).join('')||'<div class="empty">Audit qeydi yoxdur</div>'}
async function toggleOrg(id){const o=orgs.find(x=>x.id===id);const next=o.service_status==='suspended'?'active':'suspended';const{error}=await supabase.from('organizations').update({service_status:next}).eq('id',id);toast(error?error.message:`Təşkilat ${next==='active'?'aktivləşdirildi':'dayandırıldı'}`,error?'error':'success');if(!error)refresh()}
async function toggleUser(id,active){const{error}=await supabase.from('profiles').update({is_active:!active}).eq('id',id);toast(error?error.message:'İstifadəçi statusu yeniləndi',error?'error':'success');if(!error)refresh()}
async function resetPassword(authUserId){const p=prompt('Yeni müvəqqəti şifrə (ən az 8 simvol):');if(!p)return;if(p.length<8)return toast('Şifrə ən az 8 simvol olmalıdır','error');const{data,error}=await supabase.functions.invoke('admin-users',{body:{action:'reset_password',auth_user_id:authUserId,password:p}});toast(error?error.message:(data?.message||'Şifrə yeniləndi'),error?'error':'success')}
function modal(type){if(type==='org'){document.querySelector('#modal-root').innerHTML=`<div class="modal-backdrop" id="modal-bg"><form class="modal" id="org-form"><div class="modal-head"><h2>Yeni təşkilat</h2><button type="button" class="icon-btn" id="close-modal">✕</button></div><div class="form-grid"><div class="field"><label>Tam adı</label><input class="input" id="org-name" required></div><div class="field"><label>Qısa adı</label><input class="input" id="org-short" required></div><div class="field"><label>Rayon</label><select class="select" id="org-district">${districts.map(d=>`<option value="${d.id}">${escapeHtml(d.name)}</option>`).join('')}</select></div><div class="field"><label>Aylıq xidmət (AZN)</label><input class="input" id="org-price" type="number" min="0" step="0.01" value="0"></div><div class="field"><label>Növbəti ödəniş</label><input class="input" id="org-next" type="date"></div><div class="field"><label>Status</label><select class="select" id="org-status"><option value="active">Aktiv</option><option value="grace">Möhlət</option><option value="suspended">Dayandırılıb</option></select></div></div><button class="btn" style="margin-top:16px">Təşkilat yarat</button></form></div>`;document.querySelector('#close-modal').onclick=closeModal;document.querySelector('#org-form').onsubmit=createOrg;}else{document.querySelector('#modal-root').innerHTML=`<div class="modal-backdrop" id="modal-bg"><form class="modal" id="user-form"><div class="modal-head"><h2>Yeni istifadəçi</h2><button type="button" class="icon-btn" id="close-modal">✕</button></div><div class="form-grid"><div class="field"><label>Ad</label><input class="input" id="u-first" required></div><div class="field"><label>Soyad</label><input class="input" id="u-last" required></div><div class="field"><label>E-mail</label><input class="input" id="u-email" type="email" required></div><div class="field"><label>Müvəqqəti şifrə</label><input class="input" id="u-pass" minlength="8" required></div><div class="field"><label>Təşkilat</label><select class="select" id="u-org" required>${orgs.map(o=>`<option value="${o.id}">${escapeHtml(o.short_name)}</option>`).join('')}</select></div><div class="field"><label>Vəzifə</label><select class="select" id="u-position"><option value="">Seçilməyib</option>${positions.map(p=>`<option value="${p.id}">${escapeHtml(p.name)}</option>`).join('')}</select></div><div class="field"><label>Sistem rolu</label><select class="select" id="u-role"><option value="organization_admin">Organization Admin</option><option value="manager">Manager</option><option value="analyst">Analyst</option><option value="viewer">Viewer</option></select></div></div><button class="btn" style="margin-top:16px">Hesab yarat</button></form></div>`;document.querySelector('#close-modal').onclick=closeModal;document.querySelector('#user-form').onsubmit=createUser;}}
function closeModal(){document.querySelector('#modal-root').innerHTML=''}document.querySelectorAll('[data-modal]').forEach(b=>b.onclick=()=>modal(b.dataset.modal));
async function createOrg(e){e.preventDefault();const row={name:document.querySelector('#org-name').value.trim(),short_name:document.querySelector('#org-short').value.trim(),district_id:document.querySelector('#org-district').value||null,monthly_price:Number(document.querySelector('#org-price').value||0),next_payment_at:document.querySelector('#org-next').value||null,service_status:document.querySelector('#org-status').value};const{error}=await supabase.from('organizations').insert(row);toast(error?error.message:'Təşkilat yaradıldı',error?'error':'success');if(!error){closeModal();refresh()}}
async function createUser(e){e.preventDefault();const body={action:'create',email:document.querySelector('#u-email').value.trim(),password:document.querySelector('#u-pass').value,first_name:document.querySelector('#u-first').value.trim(),last_name:document.querySelector('#u-last').value.trim(),organization_id:document.querySelector('#u-org').value,position_id:document.querySelector('#u-position').value||null,system_role:document.querySelector('#u-role').value};const{data,error}=await supabase.functions.invoke('admin-users',{body});toast(error?error.message:(data?.message||'İstifadəçi yaradıldı'),error?'error':'success');if(!error){closeModal();refresh()}}
document.querySelector('#position-form').onsubmit=async e=>{e.preventDefault();const{error}=await supabase.from('positions').insert({name:document.querySelector('#position-name').value.trim(),organization_id:document.querySelector('#position-org').value||null});toast(error?error.message:'Vəzifə əlavə edildi',error?'error':'success');if(!error){e.target.reset();refresh()}};document.querySelector('#district-form').onsubmit=async e=>{e.preventDefault();const{error}=await supabase.from('districts').insert({name:document.querySelector('#district-name').value.trim()});toast(error?error.message:'Rayon əlavə edildi',error?'error':'success');if(!error){e.target.reset();refresh()}};document.querySelector('#village-form').onsubmit=async e=>{e.preventDefault();const{error}=await supabase.from('villages').insert({district_id:document.querySelector('#village-district').value,name:document.querySelector('#village-name').value.trim()});toast(error?error.message:'Kənd əlavə edildi',error?'error':'success');if(!error){e.target.reset();refresh()}};document.querySelector('#keyword-form').onsubmit=async e=>{e.preventDefault();const{error}=await supabase.from('keywords').insert({organization_id:document.querySelector('#keyword-org').value,value:document.querySelector('#keyword-value').value.trim(),kind:'phrase'});toast(error?error.message:'Açar söz əlavə edildi',error?'error':'success');if(!error){e.target.reset();refresh()}};document.querySelector('#source-form').onsubmit=async e=>{e.preventDefault();const{error}=await supabase.from('sources').insert({organization_id:document.querySelector('#source-org').value,platform:document.querySelector('#source-platform').value.trim(),url:document.querySelector('#source-url').value.trim()});toast(error?error.message:'Mənbə əlavə edildi',error?'error':'success');if(!error){e.target.reset();refresh()}};
await refresh();

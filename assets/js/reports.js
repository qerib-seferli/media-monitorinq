import{requireAuth}from'./guard.js';import{renderShell}from'./shell.js';import{supabase}from'./core.js';const c=await requireAuth();if(!c)throw 0;renderShell(c.profile,'reports');const from=document.querySelector('#from'),to=document.querySelector('#to');const now=new Date(),month=new Date(now.getFullYear(),now.getMonth(),1);from.value=month.toISOString().slice(0,10);to.value=now.toISOString().slice(0,10);async function load(){let q=supabase.from('mentions').select('source_platform,sentiment,priority_score').gte('detected_at',new Date(from.value+'T00:00:00').toISOString()).lte('detected_at',new Date(to.value+'T23:59:59').toISOString());const{data=[]}=await q;const total=data.length,neg=data.filter(x=>x.sentiment==='negative').length,pos=data.filter(x=>x.sentiment==='positive').length,critical=data.filter(x=>x.priority_score>=81).length;document.querySelector('#metrics').innerHTML=[['Ümumi',total],['Mənfi',neg],['Müsbət',pos],['Kritik',critical]].map(([l,n])=>`<div class="card metric"><div class="label">${l}</div><div class="num">${n}</div></div>`).join('');const p={};data.forEach(x=>p[x.source_platform||'Web']=(p[x.source_platform||'Web']||0)+1);document.querySelector('#platforms').innerHTML=Object.entries(p).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<div style="display:flex;justify-content:space-between;padding:10px 0;border-bottom:1px solid var(--line)"><span>${k}</span><strong>${v}</strong></div>`).join('')||'<div class="empty">Məlumat yoxdur</div>';const buckets=[['81–100',data.filter(x=>x.priority_score>=81).length],['61–80',data.filter(x=>x.priority_score>=61&&x.priority_score<81).length],['31–60',data.filter(x=>x.priority_score>=31&&x.priority_score<61).length],['0–30',data.filter(x=>x.priority_score<31).length]];document.querySelector('#priorities').innerHTML=buckets.map(([k,v])=>`<div style="display:flex;justify-content:space-between;padding:10px 0;border-bottom:1px solid var(--line)"><span>${k}</span><strong>${v}</strong></div>`).join('')}document.querySelector('#apply').onclick=load;load();
